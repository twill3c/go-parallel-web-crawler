# SECURITY.md — Go Parallel Web Crawler

外部から URL を受け取ってサーバがそこへ接続するので、**SSRF(サーバを踏み台にした内部への接続)**が
主な脅威である。SPEC §2.4 の拒否表が正本で、`internal/security` が実装、`url_test.go` が拒否表の各行を検査する。

## 守っていること

| 脅威 | 対策 | 検査 |
|---|---|---|
| localhost / 内部ホスト名 | `localhost`, `*.localhost`, `*.local`, `*.internal` を名前で拒否 | T-101 |
| ループバック・プライベート・リンクローカル・未指定・マルチキャスト・CGNAT(100.64/10)・メタデータ IP(169.254.169.254) | 名前解決した**全アドレス**を検査し、一つでも該当すれば拒否。IPv4 射影(::ffff:…)も IPv4 として判定 | T-101, T-103 |
| DNS リバインディング(検証時は公開 IP、接続時は内部 IP) | `net.Dialer.Control` で**接続直前の IP**を再検査 | DialControl の単体検査 |
| リダイレクトで内部へ | `CheckRedirect` で内部ホスト名を拒否。dial 時の再検査もそのまま効く | T-206 |
| リダイレクトで別ドメインへ | 同一ドメイン(§2.3)以外へのリダイレクトは「Redirect off-domain」で中断 | T-206 |
| リダイレクト無限 | 5 回で「Too Many Redirects」 | T-206 |
| 不正 scheme / 資格情報つき URL | http / https 以外、`user:pass@` を拒否 | T-101 |
| 大量アクセス | workers ≤ 20、maxPages ≤ 100、1 クロール ≤ 60 秒、リクエスト 5 秒、本文 2 MiB | T-302, T-210 |
| 環境のプロキシ経由の抜け道 | `Transport.Proxy = nil` | — |
| 巨大なリクエスト本文 | 4 KiB で切る | — |

## 守っていないこと(MVP のスコープ外)

- **robots.txt を読まない。** 画面の注意書き(「自分が管理するサイト、またはクロールが許可されているサイトで」)と
  上限で代替している。将来 A 案(原本 §34)で対応する
- **利用者ごとのレート制限が無い。** 一つの IP から連続で叩けば、上限内のクロールを何度でも起こせる。
  Vercel 側の関数の同時実行と 60 秒の上限で実害は小さいが、公開範囲を広げるなら前段に置く
- **公開 IP なら何でも取りに行く。** 対象が「自分の管理下か」はサーバには分からない
- **HTTPS の証明書検証は Go の既定のまま**(自己署名は失敗する。緩めない)

## テスト専用の抜け道

`GPWC_ALLOW_PRIVATE=1` を環境変数に置くと SSRF 検査が**すべて外れる**。実ブラウザ検品
(`tests/ui/browser.test.mjs`)がローカルの合成サイトをクロールするためだけにあり、
起動ログに WARNING を出す。**本番の環境変数に決して置かない。**

## 報告

脆弱性を見つけたら GitHub の Issue ではなく、リポジトリ所有者へ直接連絡してください。
